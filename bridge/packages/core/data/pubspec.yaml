name: core_data
description: core - data
publish_to: 'none'

environment:
  sdk: '>=2.19.2 <3.0.0'

dependencies:
  core_datastore:
    path: ../datastore
  core_network:
    path: ../network
  flutter:
    sdk: flutter
  model:
    path: ../model

dev_dependencies:
  very_good_analysis: ^4.0.0